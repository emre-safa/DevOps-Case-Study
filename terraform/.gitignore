.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Note: .terraform.lock.hcl is intentionally NOT ignored — it pins provider
# versions for reproducible builds (analogous to package-lock.json).
